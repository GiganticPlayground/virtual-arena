version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run setup
    build:
      commands: []
  artifacts:
    baseDirectory: docs
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
